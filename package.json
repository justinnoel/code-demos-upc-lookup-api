{
	"name": "code-demos-upc-lookup-api",
	"version": "1.0.0",
	"description": "",
	"main": "./dist/index.js",
	"scripts": {
		"build": "esbuild --bundle --sourcemap --outdir=dist ./src/index.ts",
		"dev": "miniflare --watch --debug",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"keywords": [],
	"author": "",
	"devDependencies": {
		"@cloudflare/workers-types": "^3.2.0",
		"esbuild": "0.13.10",
		"miniflare": "1.4.1"
	},
	"private": true,
	"dependencies": {
		"sunder": "^0.9.2",
		"ulidx": "^0.2.0"
	}
}
